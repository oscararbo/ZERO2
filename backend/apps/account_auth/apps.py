from django.apps import AppConfig


class AccountAuthConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.account_auth'
    verbose_name = 'Account Authentication'
